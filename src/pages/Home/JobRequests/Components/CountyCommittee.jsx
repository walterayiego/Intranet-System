import React from "react";

const CountyCommittee = ({ handleClose }) => {
  return (
    <div>
      CountyCommittee
      <button onClick={handleClose}>Close</button>
    </div>
  );
};

export default CountyCommittee;
